<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/overflowReplacement"
        android:title=""
        android:icon="@mipmap/menu_button"
        app:showAsAction="always">
        <menu>
        <item
            android:id="@+id/addProfileItem"
            android:icon="@mipmap/plus"
            android:title="Profil erstellen"
            app:showAsAction="always|withText"/>
        <item
            android:id="@+id/removeProfileItem"
            android:icon="@mipmap/minus"
            android:title="Profil löschen"
            app:showAsAction="always|withText"/>
         <item
             android:id="@+id/downloadProfiles"
             android:icon="@mipmap/download_button"
             android:title="Profil herunterladen"
             app:showAsAction="always|withText"/>
        </menu>
    </item>
</menu>
